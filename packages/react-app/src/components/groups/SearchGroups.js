import React from 'react';

// Provides group search and upload
export default function SearchGroups(props) {

  let groupContracts = props.groupsAddresses;
  let setGroupContracts = props.setGroupsAddresses;

 return (
    <div>

    </div>
    );
}